require 'spec_helper'
describe 'transmart_core' do
  context 'with default values for all parameters' do
    #it { is_expected.to compile }
    #it { should contain_class('transmart_core') }
  end
end
