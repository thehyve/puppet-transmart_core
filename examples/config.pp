# 
# An example of a manifest file which creates the transmart system user
# and generates configuration files for TranSMART.
# Services are not installed.
#

include ::transmart_core
include ::transmart_core::data
include ::transmart_core::config

